# Core bot
aiogram>=3.13,<4
aiosqlite>=0.20
python-dotenv>=1.0
aiohttp>=3.10

# Feature D — downloading (Instagram / TikTok / YouTube)
yt-dlp

# Feature A/B — search metadata & lyrics (wired in later steps)
spotipy>=2.24
lyricsgenius>=3.2
mutagen>=1.47

# Feature C — recognition (free, unofficial Shazam)
shazamio>=0.7
